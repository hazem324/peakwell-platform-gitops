apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-booking
  labels:
    app: ai-booking-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-booking-app
  template:
    metadata:
      labels:
        app: ai-booking-app
    spec:
      containers:
      - name: peakwell-ai-booking
        image: hazem231/ai-booking:v20
        ports:
        - containerPort: 8001

        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3

        livenessProbe:
          httpGet:
            path: /health/live
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 20
          timeoutSeconds: 3
          failureThreshold: 3

        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"