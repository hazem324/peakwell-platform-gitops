apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: peakwell
spec:
  storageClassName: manual   
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi