apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-symptom-predictor
  labels:
    app: ai-symptom-predictor-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-symptom-predictor-app
  template:
    metadata:
      labels:
        app: ai-symptom-predictor-app
    spec:
      containers:
      - name: peakwell-ai-symptom-predictor
        image: hazem231/ai-symptom-predictor:v8
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
